# 端到端回归记录（2026-09-22）

## 已验证

- 在现有 `127.0.0.1:7788` 服务复用扫码登录，重启后主动检查会话仍有效。
- Ctrl+C 退出时确认会话存储成功落盘，再次启动能恢复登录；服务最终保持运行。
- 南京 → 上海真实余票查询：二等座、一等座、商务座和无座显示正确，实际乘降站准确。
- 查询结果加入计划：实际乘降站、车次、日期和名称预填；取消默认早班时间限制。
- 计划内展开查询、加载反馈、余票结果、未填名称时预览日期。
- 编辑后再次新建，表单恢复新建模式。
- 已购车票查询、刷新冷却、已支付车票日历显示。
- 390px 窄屏表单与顶栏布局。
- 最终真实测试页面、隔离测试页面的浏览器控制台无错误。

在隔离 UI 服务上完成：新建 A → 保存 → 暂停 → 编辑保存仍暂停 → 新建 B，A 保留 → 取消删除 B → 确认删除 B。该服务不启动调度器、不加载真实会话、不发送消息，关闭时清理临时数据库。

## 自动回归

```sh
npm run build:server
npm run build:web
npm test
# 单独执行本次新增的 29 项回归（隔离临时数据库，无网络依赖）
npm run test:regression
# 启动隔离 UI 服务，然后访问 http://127.0.0.1:7790/#/plans
node --import tsx server/src/__test__/ui-fixture.ts
```

新增断言涵盖：计划保存、暂停状态保留、失效 ID、日期和时间格式、逆向区间、未知乘车人、独立日期预览、重试边界、暂停/删除计划不进入调度队列、UTC 与东八区起售时刻比较、当日窗口过期判断、北京时间跨日、席别字段与未知余量。

已有日期引擎与飞书签名离线测试通过。测试时未发布年份的节假日数据按现有机制降级；隔离回归使用合成日历，仅验证接口与状态逻辑，不验证法定节假日准确性。

席别字段依据：[12306 官方余票页脚本](https://kyfw.12306.cn/otn/resources/merged/queryLeftTicket_end_js.js?scriptVersion=1.95052) 中 `queryLeftNewDTO` 映射。例如二等座 30、一等座 31、商务座 32、无座 26。测试夹具独立写出这些下标，防止映射整体错位后测试仍误通过。

## 验证边界

真实账号未提交购票、支付、退票或取消订单，未主动发送飞书测试消息。真实下单、支付到期自动重购、二维码过期/重新扫码未做外部端到端验证；保留现有登录态。

暂停/删除会阻止尚未开始购票的任务；已经提交给 12306 的订单仍需用户在 12306 处理。

## 管理员、日志、分享与通知扩展（同日）

- 前后端构建、`bash -n start.sh`、`git diff --check` 通过；`npm test` 全部通过。
- 新增 `npm run test:features`：54 项检查，使用临时数据库和模拟通知传输，覆盖管理员创建/启停/密码重置、会话过期/注销/限流、HTTP 与 WebSocket 隔离、跨用户乘车人保护、调度资格、日志筛选/分页/CSV 脱敏与公式转义、旧飞书迁移及幂等性、切回单用户模式保留数据、五种平台的成功与失败响应。
- 修正 SQLite UTC 时间在浏览器中被当作本地时间的问题；日志查询与 CSV 导出显示北京时间。
- 独立 UI 服务中验证：管理员登录与退出、用户列表、普通用户隐藏管理入口及直接访问重定向；通知编辑保存并保留密钥；日志关键词筛选；车票/月历 PNG 生成、个人信息开关、390px 车票卡片布局及分享按钮可见性。
- 分享图预览为本地 1080px PNG；默认隐藏乘车人/座位，使用标准浏览器下载链接及可选 Web Share API。自动化浏览器未提供可靠的下载完成事件，实际微信客户端接收和系统分享面板未操作。
- 新版已在 `127.0.0.1:7788` 以默认单用户模式重新启动；升级前创建本地 SQLite 备份，原飞书通道迁移后保持启用，实际 12306 会话检查仍有效。

五种通知适配器仅做模拟传输测试，未向真实飞书、企业微信、钉钉、Telegram 或 Webhook 发送消息。实际接收效果取决于用户配置的平台凭证与网络；可由用户点击「发送测试」验证。

## 2026-09-22 桌面端与余票弹窗回归

- 余票使用当前出发时间段实时筛选，边界包含起止时刻；选择车次不再清空时间段。
- 隔离 UI 数据包含 07:59、08:00、08:30、09:00、09:01、18:00 六班车。默认 08:00–09:00 显示三班；通过时间选择器改为 08:30 后刷新显示两班。选择车次后筛选保留。
- 计划编辑器限制在视口高度内，内容独立滚动，余票表格内部横向滚动；实测关闭按钮、取消按钮和再次新建。关闭编辑器后，迟到的日期预览响应不能重新弹出。
- 前后端构建通过；日期引擎改用临时目录和固定假期数据，避免 CI 联网或读写用户数据库。32 项回归、54 项功能检查通过。
- macOS arm64 开发目录和打包后的 `.app` 均通过冒烟测试：随机端口后端、无凭据 HTTP 403、静态首页、SQLite、内置 Chromium 153 启动/关闭、主应用退出后后端正常退出。
- 打包 `.app` 原生 UI 检查通过：车票日历、WebSocket 实时在线、桌面设置、关闭设置、应用菜单、关闭窗口后后端继续服务、退出应用进程正常结束。测试数据放在独立临时目录；未使用真实 12306 会话、未注册电脑启动项。
- Electron 44 不会在 npm 安装时自动下载二进制，prepare 显式执行官方安装器。已用官方文档提供的镜像并保持官方 checksum 验证完成本机下载。
- electron-builder 的自动重建曾扫描到根工作区，现已禁用；桌面 SQLite 13 使用 Node-API 预编译模块，服务 SQLite 已恢复且实测可正常打开数据库。
- 静态服务依赖更新为 `@fastify/static` 10.1.4，根工作区与桌面运行依赖审计均通过。
- Windows x64、Intel Mac 的构建流程已配置，未在当前 Apple Silicon Mac 上做目标系统安装验收。真实系统登录后的自动启动、macOS 签名/公证、Windows 签名仍需在发布环境验证。
- 最终本机产物：`desktop/release/my12306-1.0.0-mac-arm64.dmg` 和同名 ZIP（约 397 MiB）；DMG 校验通过，最终打包应用冒烟通过。未配置 Developer ID，产物为未签名测试包。

## 可选签名模式

- 新增 `desktop:dist:unsigned` 与 `desktop:dist:signed`，原 `desktop:dist` 保留自动模式。Actions 手动选择模式，标签默认 unsigned，可由仓库变量覆盖。
- 6 项签名策略测试通过：未签名模式隔离继承的证书/公证变量；正式模式保留凭据并验证 macOS 公证配置；非法模式拒绝；三种模式的 builder 配置和产物命名正确。
- 本机 `desktop:dist:unsigned` 构建成功，日志确认 `identity explicitly is set to null`，未提交 Apple 公证。`-unsigned.dmg` / ZIP 已生成，DMG 完整性校验通过。
- 未签名配置下的最终 `.app` 冒烟通过：私有 HTTP、静态页面、SQLite、内置 Chromium 及正常退出。可执行文件保留 Electron 供应商的 linker ad-hoc 签名，无 TeamIdentifier，不代表 Developer ID 签名或公证。
- 未使用真实证书测试 signed 发布，未执行 Windows 安装验证；未关闭任何电脑系统防护。

## 二维码自动与手动刷新

- 默认开启过期自动刷新，可在扫码弹窗中关闭；支持手动换码、失败后重新开始。检测到扫码成功后停止刷新，等待手机确认；每次登录最多等待五分钟。
- 二维码带登录尝试 ID 与递增版本，HTTP 轮询补偿 WebSocket 丢失；取消后立即隐藏弹窗并关闭该次扫码页面，忽略迟到的图片和旧请求。
- `npm run test:qr-login` 的 10 项离线测试覆盖自动过期、手动刷新、防重复点击、扫码与刷新竞争、关闭自动刷新、超时兜底、旧码不更新、取消期间浏览器返回、前端取消/乱序响应与失败重试。前后端构建通过。
- 对照 12306 官方登录页的 `#J-code-error`、`#J-login-code-success` 状态处理过期与待确认；本次未退出真实账号以重新扫码，真实手机确认链路仍需人工验收。桌面安装包未重新构建。

## 后台运行与开机启动

- 桌面设置、托盘菜单新增「后台运行」，关闭窗口采用同一流程。后台隐藏 Dock / 任务栏图标，保留托盘入口；手动打开恢复窗口。
- Windows 的 `--autostart`、macOS 的 `wasOpenedAtLogin`、手动 `--background` 均默认隐藏启动。忽略启动期间的激活事件与重复后台启动请求；加载期间收到手动打开请求会在加载完成后显示。
- `npm --prefix desktop run test:window`：6 项启动/恢复测试通过。`desktop:prepare` 前后端构建通过；隔离原生 UI 确认桌面设置按钮、后台说明、菜单入口和退出流程。开发运行使用临时目录，未注册或修改本机真实启动项。
- 修复根目录 `desktop:start` 吞掉命令行参数的问题；使用 `npm run desktop:start -- --background` 可直接后台启动。Windows 实机、真实重启后的登录项启动仍需目标系统验收；本轮未重新生成安装包。

## 工作周起点与余票日期一致性

- 开始日期仅限定生效范围；按完整工作周挑选首个或最后一个工作日。周日补班归入紧邻的后一个工作周，周六補班计入本周末；隔周周期从首个符合生效范围的目标周固定，不随今天推进而漂移。
- 余票查询复用预览接口的首个有效出行日期；区间内没有日期时明确提示，不回退查询生效开始日。日期规则修改会清理旧结果与过期预览响应。
- 日期引擎增加周中生效、当天生效、工作周末、节假日、隔周稳定性、正负偏移、时间上下限和查询/预览一致性断言。35 项接口回归、54 项功能检查、10 项二维码测试通过；前后端构建通过。
- 隔离浏览器实测：开始日期分别选择 2026-09-23 和当天 2026-09-22，工作周第一天均查询 2026-09-28。测试接口只接受该日期；08:00–09:00 显示三班模拟车次，预览日历只在 9 月 28 日标记乘车。页面无控制台错误，未查询真实车票或下单。
- 重算时将已不符合日期规则的待执行任务标为跳过，保留已完成任务；执行前再次校验当前日期规则。日历只加载实际计算范围和锚点附近年份，避免无关年份请求拖慢预览。

## 小白用户内测分发包与本地 Ad-hoc 签名

- 重建最新 macOS arm64 未签名 DMG，包含二维码刷新、后台运行和工作周日期修复；DMG 的 `hdiutil verify` 完整性校验通过。
- 分发 ZIP 包含 DMG、中文三步说明、可双击启动助手、恢复隔离标记入口、两个底层脚本与 SHA-256 清单。安装后双击助手即可补齐本地签名、处理本应用隔离标记并打开应用；不使用 sudo、不修改全局系统安全设置。
- 单独对最外层 `.app` 执行 `codesign --force --sign -` 后，嵌套 Helper 校验仍失败。改为从最内层 Mach-O 到框架、Helper、主应用依次签名，在真实打包应用的临时副本上处理 57 个组件后，通过 `codesign --verify --deep --strict`。
- 该 Ad-hoc 副本通过私有 HTTP、静态前端、SQLite、Chromium 153 启动和退出冒烟测试。所有操作使用临时应用副本/数据目录，未修改用户已安装应用的签名、安全标记或登录项。
- 4 项隔离脚本测试通过：递归移除/恢复原值、其他元数据保留、重复运行、无标记、拒绝其他应用标识、符号链接不越出应用范围。Ad-hoc 签名不自动失效；恢复入口只恢复隔离属性，恢复原应用文件需从 DMG 重装。
- 最终 `my12306-1.0.0-mac-arm64-unsigned-bundle.zip` 约 397 MiB，内部 6 个文件和外层 ZIP 的 SHA-256 全部匹配，脚本可执行权限保留；中文文件名采用标准 ZIP UTF-8 标记，避免部分解压工具乱码。

计划详情日历与手动跳过：`npm run test:plan-status` 覆盖跳过持久化、重算/重启不重买、恢复、跨用户权限、整单待支付校验、取消成功/拒绝/结果不确定。`UI_TEST_PLAN_CALENDAR=1 UI_TEST_PORT=7791 node --import tsx server/src/__test__/ui-fixture.ts` 提供隔离交互测试；该模式取消操作为模拟，不连接 12306。
